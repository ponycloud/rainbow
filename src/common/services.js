// Generated by CoffeeScript 1.7.1
(function() {
  var options, res, s;

  res = function(pkeyName) {
    return function(data) {
      var item, _i, _len;
      data = _.toArray(JSON.parse(data));
      for (_i = 0, _len = data.length; _i < _len; _i++) {
        item = data[_i];
        item.pkey = item.desired[pkeyName];
      }
      return data;
    };
  };

  s = angular.module("rainbowServices");

  options = {
    "stripTrailingSlashes": false
  };

  s.factory("StoragePool", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/storage-pool/:storage_pool", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("StoragePoolVolume", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/storage-pool/:storage_pool/volume/:volume", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("StoragePoolDisk", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/storage-pool/:storage_pool/disk/:disk", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("id")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("id")
      }
    }, options);
  });

  s.factory("Image", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/image/:image", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("ImageVolume", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/image/:image/volume/:volume", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("Switch", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/switch/:switch", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("SwitchVnic", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/switch/:switch/vnic/:vnic", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("SwitchNetwork", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/switch/:switch/network/:network", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("SwitchNetworkRoute", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/switch/:switch/network/:network/route/:route", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("SwitchNetworkAddress", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/switch/:switch/network/:network/address/:address", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("CpuProfile", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/cpu-profile/:cpu_profile", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("Host", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("HostStoragePool", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/storage-pool/:storage_pool", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'storage_pool']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'storage_pool']")
      }
    }, options);
  });

  s.factory("HostStoragePoolHostJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/storage-pool/:storage_pool/host/:host/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'storage_pool']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'storage_pool']")
      }
    }, options);
  });

  s.factory("HostStoragePoolJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/storage-pool/:storage_pool/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'storage_pool']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'storage_pool']")
      }
    }, options);
  });

  s.factory("HostInstance", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/instance/:instance", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'instance']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'instance']")
      }
    }, options);
  });

  s.factory("HostInstanceHostJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/instance/:instance/host/:host/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'instance']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'instance']")
      }
    }, options);
  });

  s.factory("HostInstanceJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/instance/:instance/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'instance']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'instance']")
      }
    }, options);
  });

  s.factory("HostNic", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/nic/:nic", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("hwaddr")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("hwaddr")
      }
    }, options);
  });

  s.factory("HostImage", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/image/:image", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'image']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'image']")
      }
    }, options);
  });

  s.factory("HostImageHostJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/image/:image/host/:host/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'image']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'image']")
      }
    }, options);
  });

  s.factory("HostImageJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/image/:image/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'image']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'image']")
      }
    }, options);
  });

  s.factory("HostVolume", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/volume/:volume", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'volume']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'volume']")
      }
    }, options);
  });

  s.factory("HostVolumeHostJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/volume/:volume/host/:host/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'volume']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'volume']")
      }
    }, options);
  });

  s.factory("HostVolumeJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/volume/:volume/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'volume']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'volume']")
      }
    }, options);
  });

  s.factory("HostCpuProfile", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/cpu-profile/:cpu_profile", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'cpu_profile']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'cpu_profile']")
      }
    }, options);
  });

  s.factory("HostCpuProfileHostJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/cpu-profile/:cpu_profile/host/:host/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'cpu_profile']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'cpu_profile']")
      }
    }, options);
  });

  s.factory("HostCpuProfileJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/cpu-profile/:cpu_profile/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'cpu_profile']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'cpu_profile']")
      }
    }, options);
  });

  s.factory("HostDisk", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/disk/:disk", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'disk']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'disk']")
      }
    }, options);
  });

  s.factory("HostDiskHostJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/disk/:disk/host/:host/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'disk']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'disk']")
      }
    }, options);
  });

  s.factory("HostDiskJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/disk/:disk/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'disk']")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("[u'host', u'disk']")
      }
    }, options);
  });

  s.factory("HostEvent", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/event/:event", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("hash")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("hash")
      }
    }, options);
  });

  s.factory("HostBond", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/bond/:bond", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("HostBondNic", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/bond/:bond/nic/:nic", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("hwaddr")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("hwaddr")
      }
    }, options);
  });

  s.factory("HostBondRole", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/host/:host/bond/:bond/role/:role", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("User", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/user/:user", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("email")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("email")
      }
    }, options);
  });

  s.factory("UserMember", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/user/:user/member/:member", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("Disk", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/disk/:disk", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("id")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("id")
      }
    }, options);
  });

  s.factory("Config", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/config/:config", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("key")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("key")
      }
    }, options);
  });

  s.factory("Event", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/event/:event", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("hash")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("hash")
      }
    }, options);
  });

  s.factory("Tenant", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantMember", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/member/:member", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantAffinityGroup", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/affinity-group/:affinity_group", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantAffinityGroupInstance", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/affinity-group/:affinity_group/instance/:instance", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantAffinityGroupInstanceJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/affinity-group/:affinity_group/instance/:instance/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantImage", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/image/:image", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantImageVolume", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/image/:image/volume/:volume", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantQuota", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/quota/:quota", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantVolume", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/volume/:volume", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantVolumeVdisk", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/volume/:volume/vdisk/:vdisk", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantInstance", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/instance/:instance", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantInstanceVdisk", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/instance/:instance/vdisk/:vdisk", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantInstanceAffinityGroup", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/instance/:instance/affinity-group/:affinity_group", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantInstanceAffinityGroupInstanceJoin", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/instance/:instance/affinity-group/:affinity_group/instance/:instance/_join/", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantInstanceVnic", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/instance/:instance/vnic/:vnic", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantInstanceVnicAddress", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/instance/:instance/vnic/:vnic/address/:address", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantInstanceEvent", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/instance/:instance/event/:event", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("hash")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("hash")
      }
    }, options);
  });

  s.factory("TenantSwitch", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/switch/:switch", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantSwitchVnic", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/switch/:switch/vnic/:vnic", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantSwitchNetwork", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/switch/:switch/network/:network", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantSwitchNetworkRoute", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/switch/:switch/network/:network/route/:route", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

  s.factory("TenantSwitchNetworkAddress", function($resource, WEB_URL, WEB_PORT, API_SUFFIX) {
    return $resource("" + WEB_URL + API_SUFFIX + "/tenant/:tenant/switch/:switch/network/:network/address/:address", {}, {
      "patch": {
        method: "PATCH"
      },
      "list": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      },
      "query": {
        method: "GET",
        isArray: true,
        transformResponse: res("uuid")
      }
    }, options);
  });

}).call(this);
